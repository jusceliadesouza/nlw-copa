# Server

## Execução

1. Instale os pacotes utilizando `npm install`
2. Faça uma cópia do arquivo `.env.example` e renomeie para `.env`, fazendo as alterações necessárias
3. Execute `npx primsa migrate dev` para criar as migrations e executar as seeds
4. Execute `npm run dev` para iniciar o servidor 
